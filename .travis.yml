os: linux
language: node_js
node_js:
  - 10

cache: npm

before_install:
  - gem update --system
  - gem install jekyll
  - gem install bundler

before_script:
  - npm run compile
  - npm test

script:
  - ./build-master.sh
