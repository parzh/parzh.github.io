<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title><%= htmlWebpackPlugin.options.title %></title>

	<link
		href="https://fonts.googleapis.com/icon?family=Material+Icons"
		rel="stylesheet"
	>
	<link
		rel="stylesheet"
		href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
		integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh"
		crossorigin="anonymous"
	>

	<style>
		:root, body, #root {
			height: 100%;
		}
	</style>
</head>
<body>
	<div id="root">
		<script id="script_4834094162371463">
			const root = document.getElementById("root");
			const script = document.getElementById("script_4834094162371463");

			document.addEventListener("readystatechange", function readyStateChangeListener() {
				// React might not have been loaded yet
				if (document.readyState !== "complete")
					return;

				// React removes this script in src/index.tsx
				// If script exists, React hasn't been loaded
				if (root.contains(script))
					return window.location.assign("/?tried=" + encodeURIComponent(location.pathname));

				document.removeEventListener("readystatechange", readyStateChangeListener);
			});
		</script>
	</div>
</body>
</html>
